{
    "name": "claude-api-proxy",
    "version": "1.0.0",
    "description": "Cloudflare Worker for Claude API reverse proxy",
    "main": "index.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}